import Loader from "react-loader-spinner";

export default (function() {
  return (
    <Loader
      type="Rings"
      color="#00BFFF"
      height="60"
      width="60"
      className="loader"
    />
  );
});
